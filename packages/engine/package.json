{
  "name": "@crush/engine",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Pure round state machine, position math, oxygen, crush and settlement. Zero DOM, zero render imports — the same package runs as the Phase 2 server authority.",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts"
  },
  "dependencies": {
    "@crush/ledger": "*"
  }
}
