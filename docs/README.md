# docs

Per `CLAUDE.md`, this is the intended home for the two specification documents
and the decision log once the migration to the target monorepo layout is
complete.

The specs currently live in the repo root, where `CLAUDE.md` tells you to keep
them:

- `crush-depth-game-logic-v0.1.md`
- `crush-depth-acceptance-criteria-v0.1.md`

They stay there until a task explicitly moves them, so that every existing
reference in `CLAUDE.md`, `AGENTS.md` and `ARCHITECTURE.md` keeps resolving.

- [`decisions/`](./decisions) — decision log.
- [`test-plans/`](./test-plans) — milestone-specific manual browser checklists.
- [`DESCENT-PLAN.md`](./DESCENT-PLAN.md) — current delivery roadmap and milestone status.
